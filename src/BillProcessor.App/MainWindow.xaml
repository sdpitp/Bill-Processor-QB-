<Window x:Class="BillProcessor.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Vendor Bill Processor (QuickBooks Desktop)"
        Height="720"
        Width="1280"
        MinHeight="560"
        MinWidth="980">
    <DockPanel Margin="12">
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,10">
            <WrapPanel Margin="0,0,0,8">
                <Button Content="Import CSV" Width="110" Margin="0,0,8,0" Click="ImportCsvClick"/>
                <Button Content="Add Bill" Width="90" Margin="0,0,8,0" Click="AddBlankBillClick"/>
                <Button Content="Normalize + Validate" Width="155" Margin="0,0,8,0" Click="NormalizeAndValidateClick"/>
                <Button Content="Queue QB Post" Width="120" Margin="0,0,8,0" Click="QueueToQuickBooksClick"/>
                <Button Content="Verify QB Results" Width="130" Margin="0,0,8,0" Click="VerifyQuickBooksResultsClick"/>
                <Button Content="Save Securely" Width="110" Margin="0,0,8,0" Click="SaveClick"/>
                <Button Content="Reload Saved" Width="110" Margin="0,0,8,0" Click="ReloadClick"/>
                <Button Content="Clear Grid" Width="90" Click="ClearGridClick"/>
            </WrapPanel>
            <WrapPanel VerticalAlignment="Center">
                <CheckBox x:Name="AuthorizePostingCheckBox" Margin="0,0,12,0" VerticalAlignment="Center" Content="Authorize QB posting for this session"/>
                <TextBlock VerticalAlignment="Center" Margin="0,0,6,0" Text="Transport:"/>
                <ComboBox x:Name="QuickBooksModeComboBox"
                          Width="220"
                          Margin="0,0,12,0"
                          SelectedIndex="0"
                          SelectionChanged="QuickBooksModeSelectionChanged">
                    <ComboBoxItem Tag="FileDropBridge">File Drop Bridge</ComboBoxItem>
                    <ComboBoxItem Tag="DirectDesktopSdk">Direct Desktop SDK (QBXMLRP2)</ComboBoxItem>
                </ComboBox>
                <TextBlock VerticalAlignment="Center" Margin="0,0,6,0" Text="Company File:"/>
                <TextBox x:Name="CompanyFileTextBox" Width="210" Margin="0,0,12,0" Text="PrimaryCompany"/>
                <TextBlock x:Name="QuickBooksPathsTextBlock" VerticalAlignment="Center" Foreground="#334155"/>
            </WrapPanel>
        </StackPanel>

        <Border DockPanel.Dock="Bottom" Background="#1E293B" CornerRadius="6" Padding="10" Margin="0,10,0,0">
            <TextBlock x:Name="StatusTextBlock" Foreground="White" Text="Ready." FontSize="13"/>
        </Border>

        <DataGrid x:Name="BillsGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  RowHeaderWidth="0"
                  GridLinesVisibility="Horizontal"
                  SelectionMode="Single"
                  SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Vendor" Binding="{Binding VendorName}" Width="190"/>
                <DataGridTextColumn Header="Invoice #" Binding="{Binding InvoiceNumber}" Width="130"/>
                <DataGridTextColumn Header="Invoice Date" Binding="{Binding InvoiceDate, StringFormat={}{0:yyyy-MM-dd}}" Width="115"/>
                <DataGridTextColumn Header="Due Date" Binding="{Binding DueDate, StringFormat={}{0:yyyy-MM-dd}}" Width="115"/>
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat={}{0:F2}}" Width="95"/>
                <DataGridTextColumn Header="Expense Account" Binding="{Binding ExpenseAccountName}" Width="150"/>
                <DataGridTextColumn Header="PO/Job Raw" Binding="{Binding PurchaseOrderOrJobRaw}" Width="150"/>
                <DataGridTextColumn Header="PO/Job (6-digit)" Binding="{Binding PurchaseOrderOrJobNormalized}" Width="130" IsReadOnly="True"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110" IsReadOnly="True"/>
                <DataGridTextColumn Header="QB Req ID" Binding="{Binding QuickBooksRequestId}" Width="185" IsReadOnly="True"/>
                <DataGridTextColumn Header="QB Txn ID" Binding="{Binding QuickBooksTxnId}" Width="130" IsReadOnly="True"/>
                <DataGridTextColumn Header="QB Attempts" Binding="{Binding QuickBooksPostAttemptCount}" Width="90" IsReadOnly="True"/>
                <DataGridTextColumn Header="Validation Errors" Binding="{Binding ValidationErrorText}" Width="180" IsReadOnly="True"/>
                <DataGridTextColumn Header="QB Last Error" Binding="{Binding QuickBooksLastErrorMessage}" Width="*" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
